# Reviews bot shield

The public reviews API uses a short shared cache, bounded pagination, per-IP limits, and a conservative scripted-client gate. Authenticated review submission remains separately rate-limited.
